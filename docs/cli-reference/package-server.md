---
title: "package server"
weight: 82
description: "Run a local package server"
---

## Overview

Runs a local web server serving packages from the cache. This is run automatically in the background when `podplane local start` is used, and stopped on `podplane local stop` of the last running VM.

```
podplane package server [flags]
```
